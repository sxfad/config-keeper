spring:
  application:
    name: config-keeper-demo2

  profiles:
    active: ${profile:dev}

server:
  port: 8088
  #servlet:
  #  context-path: /test

suixingpay:
  config:
    cache-path: ${APP_HMOE:.}/config/
management:
  endpoints:
    web:
      base-path: /ops
      exposure:
        include: '*'

  server:
    #port: 9090
    #servlet:
    #  context-path: /mbase


